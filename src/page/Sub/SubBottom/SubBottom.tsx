import React from "react";
import { DataType } from "../../../data/data";

const SubBottom = ({ thisData }: { thisData: DataType }) => {
  console.log(thisData);
  return <div>SubBottom</div>;
};

export default SubBottom;
